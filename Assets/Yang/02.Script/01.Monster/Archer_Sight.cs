using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Archer_Sight : MonoBehaviour {

    //private Archer parent;
    //private Trap trap;
    //private void Start()
    //{
    //    parent = gameObject.GetComponentInParent(typeof(Archer)) as Archer;
    //}
    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    if (collision.tag == "Trap_T" && parent.M_Attack>0)
    //    {
    //        trap = collision.transform.GetComponent<Trap>();

    //        Vector3 vec1 = parent.transform.position;
    //        Vector3 vec2 = trap.transform.position;
    //        if (vec1.x<vec2.x && vec1.y<vec2.y)
    //        {
    //            parent.iState = 1;
    //            //trap.hp -= parent.M_Attack;
    //            parent.m_ct = parent.M_Cooltime;
    //        }

    //    }
    //}
}
